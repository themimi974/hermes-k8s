# TEMPLATE — values are replaced at deploy time by the deploy skill.
# Never commit real credentials here.
# POSTGRES_DB must be "hermes_dashboard" (matches API config).
apiVersion: v1
kind: Secret
metadata:
  name: postgres-credentials
  namespace: dashboard
type: Opaque
stringData:
  POSTGRES_USER: "REPLACE_ME"
  POSTGRES_PASSWORD: "REPLACE_ME"
  POSTGRES_DB: "hermes_dashboard"
